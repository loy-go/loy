apiVersion: v1
kind: Service
metadata:
  name: {{.ProjectName}}
  labels:
    app: {{.ProjectName}}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: {{.ProjectName}}
